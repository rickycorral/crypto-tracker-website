import React from 'react';
import ReactDOM from 'react-dom';
import CryptoTracker from './App';

import './styles.css';

ReactDOM.render(
  <React.StrictMode>
    <CryptoTracker />
  </React.StrictMode>,
  document.getElementById('root')
);
